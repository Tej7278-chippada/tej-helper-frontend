{
    "env": {
      "serviceworker": true
    },
    "rules": {
      "no-restricted-globals": ["error", {
        "name": "self",
        "message": "Use `self` explicitly for service worker context"
      }]
    }
  }